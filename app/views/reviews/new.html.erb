<h1>Add review for <%= @restaurant.name %></h1>

<div class="w-50">

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, as: :text %>
  <%= f.input :rating, as: :range, collection: 0..5 %>
  <%= f.button :submit %>
<% end %>
</div>
